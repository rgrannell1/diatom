import { firstMatch } from "./first-match.ts";
import { firstCaptureGroup } from "./first-capture-group.ts";
import { eachMatch } from "./each-match.ts";

export default {
  firstMatch,
  firstCaptureGroup,
  eachMatch,
};
